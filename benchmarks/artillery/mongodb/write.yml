config:
  target: 'http://localhost:3000'
  phases:
    - duration: 30
      arrivalRate: 10
  defaults:
    headers:
      Content-Type: application/json
scenarios:
  - name: MongoDB Write
    flow:
      - post:
          url: '/benchmark/mongodb/write'
