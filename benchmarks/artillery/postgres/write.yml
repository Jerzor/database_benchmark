config:
  target: 'http://localhost:3000'
  phases:
    - duration: 60
      arrivalRate: 40
  defaults:
    headers:
      Content-Type: application/json
scenarios:
  - name: Postgres Write
    flow:
      - post:
          url: '/benchmark/postgres/write'
